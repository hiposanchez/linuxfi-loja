<% if logado? %>
<p> Olá <%= usuario_atual.nome %></p>
<p> <%= link_to( "Logoff", 
  sessao_path,
  :method => :delete,
  :confirm => "Tem certeza de que deseja sair da aplicação."
) %> </p>
<% else %>
<p>
  <%= link_to( "Faça login na aplicação", sessao_path) %>
  |
  <%= link_to( "cadastre-se na aplicação", usuario_path) %>
</p>
<% end  %>

<ul>
      <li><%=
      #Em link_to pode-se passar um hashe contendo atributos que seriam passados para a âncora no html
      link_to( "Produto", produtos_path ) %>
      </li>
      <% unless pedido_atual.blank? %>
        <li>
          <%= link_to( "Veja o seu carrinho", itens_path ) %>
        </li>
      </ul>
    <%  end %>